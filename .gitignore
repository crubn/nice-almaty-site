# Local tooling / secrets — NEVER commit
.vercel
.gstack/

# Occupancy table holds residents' names (ФИО) — keep it local only.
# Regenerate data/availability.json from it with scripts/sync_availability.py.
Общежитие Список.xlsx

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
node_modules/
