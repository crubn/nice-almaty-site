# Internal gstack runtime files (contain local tokens) — never deploy.
.gstack
# Netlify config is irrelevant on Vercel.
netlify.toml
# Source table (may contain staff names) — NEVER deploy it.
# The chatbot reads data/*.json instead (generated by scripts/sync_availability.py).
Чат бот.xlsx
Общежитие Список.xlsx
